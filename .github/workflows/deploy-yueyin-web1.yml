name: Deploy Frontend Web2 (yueyin-houtai-qianduan)

on:
  workflow_dispatch:
  push:
    branches:
      - main
      - master
    paths:
      - '**'
      - '.github/workflows/deploy-yueyin-web1.yml'

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20.19.0'
          cache: npm
          cache-dependency-path: package-lock.json

      - name: Install Dependencies
        run: |
          if [ ! -f package-lock.json ]; then
            echo "ERROR: package-lock.json not found. Please run 'npm install' locally and commit package-lock.json." >&2
            exit 1
          fi
          npm ci

      - name: Build
        run: npm run build

      - name: Clean Target Directory
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: root
          password: ${{ secrets.SERVER_PASSWORD }}
          port: 22
          script: |
            set -e
            mkdir -p /www/wwwroot/yueyin-web2
            rm -rf /www/wwwroot/yueyin-web2/*

      - name: Upload Static Files
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: root
          password: ${{ secrets.SERVER_PASSWORD }}
          port: 22
          source: dist/*
          target: /www/wwwroot/yueyin-web2
          strip_components: 1
          overwrite: true

      - name: Fix Permissions
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: root
          password: ${{ secrets.SERVER_PASSWORD }}
          port: 22
          script: |
            set -e
            chown -R www:www /www/wwwroot/yueyin-web2
